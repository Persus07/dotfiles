"Persus07 - 2025.05.10

" No necesita ser compatible con Vi
set nocompatible 

" Codificación
set encoding=utf-8 " 

" Obliga modo de colores
set t_Co=256

" Muestra el numero de linea
set number

" Muestra el estado del archivo en la esquina inferiors
set ruler

" Que vaya rapido en tty
set ttyfast

" No suena una campana 
set noerrorbells
set novisualbell
set t_vb=
set tm=500

" Crear identación de forma automatica
set autoindent
set smartindent

" Muestra el otro parentesis o corchete
set showmatch

" Mejora la forma de buscar
set ignorecase
set smartcase
set hlsearch

" Si el texto se pasa de la linea, hace cascada a la siguiente linea
set wrap

" Activa el coloreado de sintaxis
syntax on 

" Coloca identación
filetype indent on

" Mi esquema de colores favorito
colorscheme jellybeans
